---
- hosts: node1
  tasks:
    - name: install nginx
      ansible.builtin.apt: name=nginx state=latest
    - name: start nginx
      ansible.builtin.service:
          name: nginx
          state: started
