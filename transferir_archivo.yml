---
- name: Copy file to node1 using command module
  hosts: localhost
  become: true
  tasks:
    - name: Copy file using shell module
      ansible.builtin.shell: "scp -i ~/.ssh/id_rsa ~/utp/utp.txt root@10.128.0.21:/home/root"
